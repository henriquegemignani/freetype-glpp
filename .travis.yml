language: cpp

install:
  - sudo add-apt-repository -y ppa:henriquegemignani/uspgamedev
  - sudo apt-get update
  - sudo apt-get install cmake

compiler:
  - gcc
  - clang

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
